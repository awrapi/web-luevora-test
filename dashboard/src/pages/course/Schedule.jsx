/** Schedule — Calendar view + student schedule management + follow-up queue */
const Schedule = () => (
  <div className="page page--schedule">
    <h2>Jadwal Kalender</h2>
    {/* TODO: Tab 1 — Kalender (monthly view with events) */}
    {/* TODO: Tab 2 — Jadwal Siswa (from customer_schedules table) */}
    {/* TODO: Create/edit schedule modal with label + manual contact picker */}
    {/* TODO: Follow-up queue progress bar */}
    {/* Legacy: schedule.php UI + api_schedule.php (all actions) */}
  </div>
);
export default Schedule;
