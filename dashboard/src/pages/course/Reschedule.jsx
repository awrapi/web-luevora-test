/** Reschedule — Manage reschedule requests (approve/reject with WA notification) */
const Reschedule = () => (
  <div className="page page--reschedule">
    <h2>Reschedule Requests</h2>
    {/* TODO: Filter tabs — Pending, Approved, Rejected */}
    {/* TODO: Request detail popup with affected schedules + chat history */}
    {/* TODO: Approve options — Move All vs Create New */}
    {/* Legacy: api_reschedule.php (fetch_requests, fetch_request_detail, approve_reschedule, reject_reschedule) */}
  </div>
);
export default Reschedule;
