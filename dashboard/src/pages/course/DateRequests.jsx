/** Date Requests — Manage new date/schedule requests with smart merge detection */
const DateRequests = () => (
  <div className="page page--date-requests">
    <h2>Request Tanggal Baru</h2>
    {/* TODO: Filter tabs — Pending, Approved, Merged, Rejected */}
    {/* TODO: Smart merge UI — conflict detection with existing schedules/other requests */}
    {/* TODO: Approve (standalone), Merge with schedule, Merge requests, Reject */}
    {/* Legacy: api_date_request.php (fetch_requests, fetch_request_detail, approve_request, merge_with_schedule, merge_requests, reject_request) */}
  </div>
);
export default DateRequests;
