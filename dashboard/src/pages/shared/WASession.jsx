/** WhatsApp Session — Session management (start/stop/status) */
const WASession = () => (
  <div className="page page--wa-session">
    <h2>WhatsApp Session</h2>
    {/* TODO: Session status, QR code display, start/stop buttons */}
    {/* Legacy: api_admin.php wa_session_status, start_wa_session */}
  </div>
);
export default WASession;
